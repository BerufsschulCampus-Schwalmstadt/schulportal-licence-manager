{
  "dependencies": {
    "concurrently": "^7.6.0"
  },
  "scripts": {
    "dev": "run-s npm start --prefix ./client npm run devStart --prefix ./server",
    "start": "npm run start --prefix ./server"
  }
}

{
  "dependencies": {
    "concurrently": "^7.6.0"
  },
  "scripts": {
    "start": "concurrently \"npm start --prefix ./client\" \"npm run devStart --prefix ./server\"",
    "build": "npm install && npm install --prefix ./client && npm install --prefix ./server",
    "dev": "npm run start --prefix ./server"
  }
}

{
  "dependencies": {
    "concurrently": "^7.6.0"
  },
  "engines": {
    "node": "18.x"
  },
  "scripts": {
    "start": "npm run devStart --prefix ./backend",
    "devStart": "concurrently \"npm start --prefix ./client\" \"npm run dev --prefix ./server\"",
    "build": "npm install && npm install --prefix ./backend",
    "dev": "npm run start --prefix ./server"
  }
}

{
  "dependencies": {
    "concurrently": "^7.6.0"
  },
  "scripts": {
    "start": "run-s npm start --prefix ./client npm run devStart --prefix ./server",
    "dev": "npm run start --prefix ./server"
  }
}

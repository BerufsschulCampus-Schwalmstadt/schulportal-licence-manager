{
  "dependencies": {
    "concurrently": "^7.6.0"
  },
  "scripts": {
    "build": "cd client && npm install && npm run build",
    "start": "concurrently \"node server/scripts/dist/server.js\" \"cd client && npm start\""
  }
}

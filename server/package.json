{
  "dependencies": {
    "assert": "^2.0.0",
    "body-parser": "^1.20.1",
    "convert-array-to-csv": "^2.0.0",
    "cors": "^2.8.5",
    "csv": "^6.2.5",
    "dotenv": "^16.0.3",
    "ejs": "^3.1.8",
    "express": "^4.18.2",
    "fs": "^0.0.1-security",
    "puppeteer": "^19.3.0"
  },
  "devDependencies": {
    "@types/convert-array-to-csv": "^2.0.0",
    "@types/cors": "^2.8.13",
    "@types/express": "^4.17.14",
    "@types/node": "^14.11.2",
    "gts": "^3.1.1",
    "nodemon": "^2.0.20",
    "typescript": "~4.7.0"
  },
  "scripts": {
    "build": "cd ../ && cd client && npm install && npm run build",
    "client-install": "npm install --prefix client",
    "start": "node scripts/dist/server.js",
    "dev": "concurrently \"npm run server\" \"npm run client\"",
    "server": "nodemon scripts/dist/server.js",
    "client": "npm start --prefix client",
    "lint": "gts lint",
    "clean": "gts clean",
    "compile": "tsc",
    "fix": "gts fix",
    "prepare": "npm run compile",
    "pretest": "npm run compile",
    "posttest": "npm run lint"
  }
}
